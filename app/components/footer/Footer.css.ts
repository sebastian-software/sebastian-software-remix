import { style } from "@vanilla-extract/css";

export const root = style({
  background: "#333",
  color: "#999",
  padding: "1rem",
});

export const content = style({
  display: "flex",
  gap: "1rem",
});
